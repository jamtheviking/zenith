<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <EditText
        android:id="@+id/routineTitleEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Routine Title" />

    <EditText
        android:id="@+id/routineDetailsEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Routine Details" />

    <EditText
        android:id="@+id/routineTimeEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Time of Execution (HH:MM AM/PM)"
        android:focusable="false"
        android:clickable="true" />

    <Button
        android:id="@+id/saveRoutineButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Save Routine" />
</LinearLayout>
